meta {
  name: Request a password reset
  type: http
  seq: 7
  tags: [
    Auth
  ]
}

post {
  url: {{baseUrl}}/api/v1/auth/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{demoEmail}}"
  }
}

tests {
  test("password reset request always returns the generic response", function () {
    expect(res.getStatus()).to.equal(200);
    const body = res.getBody();
    expect(body.success).to.equal(true);
    expect(body.message).to.equal("If the email is registered, a reset link has been sent.");
    expect(body.data).to.deep.equal({});
  });
}

example {
  name: 200 Response
  description: Reset link sent if the email is registered.

  request: {
    url: {{baseUrl}}/api/v1/auth/forgot-password
    method: POST
    mode: json
    body:json: {
      {
        "email": ""
      }
    }
  }

  response: {
    headers: {
      Content-Type: application/json
    }

    status: {
      code: 200
      text: OK
    }

    body: {
      type: json
      content: '''
        {
          "success": false,
          "message": "",
          "data": {}
        }
      '''
    }
  }
}

example {
  name: 400 Response
  description: Error response.

  request: {
    url: {{baseUrl}}/api/v1/auth/forgot-password
    method: POST
    mode: json
    body:json: {
      {
        "email": ""
      }
    }
  }

  response: {
    headers: {
      Content-Type: application/json
    }

    status: {
      code: 400
      text: Bad Request
    }

    body: {
      type: json
      content: '''
        {
          "success": false,
          "message": "",
          "errors": [
            {
              "field": "",
              "message": ""
            }
          ]
        }
      '''
    }
  }
}

example {
  name: 429 Response
  description: Error response.

  request: {
    url: {{baseUrl}}/api/v1/auth/forgot-password
    method: POST
    mode: json
    body:json: {
      {
        "email": ""
      }
    }
  }

  response: {
    headers: {
      Content-Type: application/json
    }

    status: {
      code: 429
      text: Too Many Requests
    }

    body: {
      type: json
      content: '''
        {
          "success": false,
          "message": "",
          "errors": [
            {
              "field": "",
              "message": ""
            }
          ]
        }
      '''
    }
  }
}
